/**
 * MyCylinder2
 * @constructor
 */
class MyCylinder2 extends CGFobject
{
	constructor(scene, base, top, height, slices, stacks)
	{
		super(scene);

		this.base = base;
		this.slices = slices;
		this.height = height;
		this.top = top;
		this.stacks = stacks;
	};

	
};
